import styled from "styled-components";

const Header = styled.header`
  background-color: transparent;
  position: fixed;
  padding: 15px 0;
  top: 0;
  left: 0;
  right: 0;
  z-index: 99999;
`
export const S = {
  Header
}